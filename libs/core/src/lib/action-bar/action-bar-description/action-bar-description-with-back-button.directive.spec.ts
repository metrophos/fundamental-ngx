import { ActionBarDescriptionWithBackButtonDirective } from './action-bar-description-with-back-button.directive';

describe('ActionBarDescriptionWithBackButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionBarDescriptionWithBackButtonDirective();
    expect(directive).toBeTruthy();
  });
});
