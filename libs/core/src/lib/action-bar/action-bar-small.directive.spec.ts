import { ActionBarSmallDirective } from './action-bar-small.directive';

describe('ActionBarSmallDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionBarSmallDirective();
    expect(directive).toBeTruthy();
  });
});
